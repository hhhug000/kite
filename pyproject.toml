[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kite"
version = "0.1.0"
description = "Known Identity Traversal Engine"
readme = "README.md"
license = { file = "LICENSE" }
authors = [ { name = "hhhug000" } ]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: Apache Software License",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/hhhug000/kite"
